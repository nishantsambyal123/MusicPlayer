/**
 * @format
 */

import {AppRegistry} from 'react-native';
import HomeScreen from './app/src/components/HomeScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => HomeScreen);
